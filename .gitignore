.archive
.archive/
.archive/*

# Compiled apt-repo dev server. Regenerated by `make pages` / `make serve`
# from main.go. The generated HTML pages (index.html, tinygo/index.html)
# stay tracked because github pages serves them.
/apt-repo
pkg/
